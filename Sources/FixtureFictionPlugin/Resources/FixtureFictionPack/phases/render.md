# Render

Compile provider-neutral PromptIR and route each generated or AI-enhanced shot to an exact provider request envelope. Preserve the Shot List source path for AI enhancement. Imported shots stay in the skipped list and never create provider requests. CI stops at the request envelope and never submits a billable generation.
