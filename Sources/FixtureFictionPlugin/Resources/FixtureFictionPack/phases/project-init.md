# Project Init

Own the fiction intake. Record either a greenfield premise with explicit constraints or an existing screenplay path plus a faithful premise. Never invent a missing plot and never overwrite imported screenplay bytes. Write `project_init` with `write_phase_extension`; the closed schema is the canonical source decision.
