# Frames

Create frame requests only for generated shots whose keyframe strategy needs anchors. Start means exact t=0; end means exact terminal state. AI-enhanced and imported shots must appear only in the skipped list. An imported-only project has a valid empty generated-frames array and spends nothing.
