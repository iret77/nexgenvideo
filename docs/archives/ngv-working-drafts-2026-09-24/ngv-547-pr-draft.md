# Add complete 3.4 knowledge retrieval and correct four-roll assessment

The supplied production knowledge 3.4 corpus was documented but unavailable to runtime tools, and a four-roll batch with three same-axis failures was treated as successful if one candidate passed. This change adds a separate hashed source archive to the existing knowledge loader/tool and corrects that iteration assessment.

All 297 sections, 682 source blocks, 42 stable blueprints, ten runbooks, 59 tables and 21 templates are addressable with exact byte receipts. Technique plans select one A/B/C shape; B/C read shared lint units without A's form, and C retrieves one selected medium plus its complete form and exceptions. Provider/backend source claims remain dated, non-executable evidence. Legacy libraries, exact pins and public V1 stored layouts retain their meaning.

This is a partial implementation of #547, not its completion. Persisted project technique, economy compiler/semantic slate, protected repair, draft rescue, typed animatic, updated style/format consumers and actual backend cold-test traces remain open. The runtime status document enumerates all nine points and distinguishes the locked sparse-projection decision from merely unfinished implementation. An automatic approval review rejected the broader compiler/writer/gate/lineage/pack integration before execution; it is not included here.

Validation: static diff/whitespace checks only. Actions tests for archive coverage, exact unit reads, technique isolation, tool receipts, legacy preservation and four-roll counterexamples are prepared but unexecuted. No local build/test, generation, CI dispatch, merge or release. New knowledge resources are independently versioned; no isolated host/pack release bump.

Local commit: 20d6b14351c040ff420d7d6f62e3557f9535053d
