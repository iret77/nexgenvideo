Du bist IMPLEMENTIERUNGS-LEAF fuer NexGenVideo Issue #532. Nur der Parent darf nicht implementieren; du sollst selbst implementieren. NIEMALS ai-fleet, Agents/Subagents oder weitere AI-CLIs starten, auch keine Reviewer. Lies AGENTS.md, basic-memory und docs/CONCEPT.md; gesperrte Vertraege/Pack-ABI bleiben unveraendert. Parent uebernimmt den direkten read-only Opus-Review.

Issue samt aktuellem Body/Handoffs liegt in /tmp/ngv-532-current.json. Setze den gesamten konkreten Fix- und Abnahmeumfang um: Resume darf abgelehnte Writes nicht als gespeichert/fertig zeigen, unbepreiste Batches nicht als freigabebereit. Host-owned strukturierte Zustandstrennung Entwurf/Write abgelehnt/persistiert/geprueft/freigegeben; keine Promptdisziplin als einzige Sicherung. Agent behielt strukturierte Gateursachen; Nutzer sieht knappe Auswirkung + erforderliche Handlung, aufklappbare Diagnose fuer Toolnamen/Pfade/Fingerprints. Recovery-/Resubmit-Spam zwischen Rich Result, Chat und Dock unterdruecken, echte kreative Aenderungen mit pruefbarer Diff-Zustimmung beibehalten. KEIN Autoapprove, keine Byteidentitaet ohne exakten Hostvergleich. App-Lokalisierung fuer beide Backendpfade; keine app-authored Meldung als Userturn. Abnahme: Resume->Referenzkopie->abgelehnter Writer->Recovery->Zonevalidierung->erfolgreicher Writer->Batch ohne Schaetzung, hostzustandsgesteuerte Controls und Anzeigen. Die Lineage-Kopierursache gehoert #531 und darf hier nicht dupliziert werden. 'Hier keine Implementierung' am Ende des alten Issue-Texts beschreibt dessen damalige Aufnahme; der aktuelle Ownerauftrag lautet ausdruecklich Umsetzung aller offenen Issues und autorisiert diese Implementierung.

Stack: codex/issue-532 baut auf lokalem codex/issue-455 f063dc7a54d1ada809c2c75045b32d3f165d103f (PR #601) auf, damit der gemeinsame Runtimevertrag erhalten bleibt. Diese Basis hat noch KEIN gruenes CI nach dem Macro-Compilefix; das ist offen, nicht voraussetzen. Kein Cherry-pick/Merge anderer unfertiger Stacks. Nutze vorhandene gemeinsame Spend/Writer/Agent-Projektion, kein UI-Gesamtredesign. AppTheme verwenden. Falls exakte Integration in einen anderen Stack erforderlich wird, als konkrete Abhaengigkeit dokumentieren, nicht fremde Worktrees veraendern. Bei notwendiger Locked-Contract-/ABI-Abweichung stoppen und Entscheidung vorlegen.

Keine lokalen Builds, Tests, App-Starts, Devserver. Nur gezielte Regressionen und native Actions-Evidenz VORBEREITEN, nicht ausfuehren. KEIN Push/PR/CI/Merge/Release, keine Provider-/Modellcalls und keine Hostkonfiguration. Alle bestehenden Aenderungen erhalten. Lokal implementieren und committen mit erforderlicher Git-Eskalation; /tmp/ngv-532-completion.md mit sauberem Commit, Kriterium->Code->vorbereiteter Evidenz->ausstehender Runtime Matrix, PR-Entwurf, ehrlichen Blockern und Batch-Versionsausnahme liefern. Keine 'erfuellt/verifiziert'-Behauptung fuer ungetestete Flows. Nicht wiederholt grosse Logs/Diffs lesen/dumpen. Bei Spawn-/Policyblocker genau melden und stoppen. TLDR.
