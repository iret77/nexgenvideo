Du bist IMPLEMENTIERUNGS-LEAF fuer #534 Mirelo Reviewkorrekturen. Nur der Parent darf nicht implementieren; du sollst selbst implementieren. NIEMALS ai-fleet, Agents/Subagents oder weitere AI-CLIs starten, auch keine Reviewer. Lies AGENTS.md/basic-memory. Erhalte alle bisherigen Aenderungen und den Stack. Parent macht anschliessend direkten Read-only-Opus-Delta-Review.

Lies gezielt /tmp/ngv-534-completion.md, /tmp/ngv-534-parent-review.md und /tmp/ngv-534-review-baseline.json. Opus5.5/high war erfolgreich ohne Denials, geprueft wurde der eingefrorene /tmp/ngv-534-review-baseline.diff waehrend der vorige Leaf noch Fixtures ergaenzte. Revalidiere ALLE Findings am jetzigen Commit; bereits korrigierte nicht doppelt bearbeiten. Parent hat compile/spend gate/race/results/source identity am echten Code bestaetigt. Keine riesigen alten Logs/Diffs wiederholt ausgeben.

Bestaetigter Korrekturumfang:
1. Fehlendes required alternatives-Argument bei withSpendApproval beheben. Weitere enge Compile-/Enum-Luecken statisch pruefen.
2. Raw-Prompt darf PromptCompiler.enforceGate/pro-toggle nicht umgehen: agentPrompt allein reicht nicht. Dieselbe funktionale Gatepruefung wie GenerationRequest, und gezielte Regression toggle-off vor Upload/Preflight/Create. Keine rohe Inhaltsgeneration fuer normale Agentpfade.
3. Snapshot der Arbeitskopie muss vor JEDEM Editor-/Projektwrite nach await validiert werden. Projektwechsel waehrend Download/mehrerer Imports darf kein Ergebnis im neuen Projekt hinterlassen. Deterministische suspendierbare Downloadfixture.
4. Ein logischer Job identifiziert immutable Nutzerrequest. completed darf veraenderte operation/model/prompt/options/source IDs nicht still als alten Erfolg zurueckgeben. Zugleich muss bereits angenommener bezahlter Job nach Restart OHNE fluechtigen Compiletoken/frischen Katalog/noch vorhandene Quellenbytes wieder polled/installiert werden koennen. Explizite sichere Resume-Semantik/immutable Intentvergleich, keine zweite Freigabe oder Create fuer fertigen Providerjob. Keine gegensaetzlichen Schnellfixes, die einen der beiden Vertrage verletzen.
5. Actor-Reentrancy: gleichzeitige execute-Aufrufe sollen denselben logischen Task teilen; MIDI angenommene ID darf durch concorrentes submitting->unknown/CAS nicht verloren gehen. V3 Idempotency ist kein Grund unkoordiniert mehrere Creates zu senden. 408/409 oder sonstige unklare Annahme nicht als definitive Ablehnung mit Budget-release behandeln; exakte sichere Fehlerklassifikation. Regression zwei Calls um gegateten Create/Poll, keine doppelten Imports/Ausgaben. Persistenter Recoverypfad bleibt crashsicher, keine automatische MIDI-Doppelabrechnung.
6. Stabile strukturierte MIDI-Artefakte duerfen keine wechselnden signierten Ergebnis-URLs als fachliche Bytes enthalten. Refresh desselben Jobs muss idempotent installierbar bleiben, auch nach Teilimport/Crash. NoteJSON/Scoremanifest/Audioinstallation gemeinsam auf sichere Wiederaufnahme pruefen, keine stillen Ueberschreibungen fachlich abweichender Bytes.
7. Eindeutige hostbestimmte PDF-Dateinamen verhindern Kollisionen; '.'/'..'/falsche Extensions wurden inzwischen teilweise korrigiert. Lesbare Importnamen statt UUID erhalten (voriger Leaf nutzt inzwischen stagingRoot/descriptor.filename, revalidieren).
8. Rein lokale Options-/Format-/Zeit-/Dauervalidierung VOR externem Quellenupload; fuer Preflight notwendiger kostenloser Upload bleibt passend.
9. Downloadfehler nicht pauschal als abgelaufene URL tarnen. Nur evidenzbasierte Ablaufantworten refreshen, Cancellation/Offline/Policy/Size/Lokaldisk korrekt weitergeben, zweiter Ablauf actionable. Keine neue Erstellung fuer Refresh.
10. Neue Budgetreservation bei approve-Fehler sauber freigeben, bei unbekannter Providerannahme NICHT freigeben. Leerem errors-Array muss brauchbare Statusfehlermeldung folgen.
11. Fixtures muessen httpBodyStream mit auswerten, keine Annahme request.httpBody sei bei URLProtocol gesetzt. Zielfuehrende Regressionen fuer oben, keine Implementierungsspiegel-Tests.

KEIN lokaler Build/Test/Appstart/Devserver, KEIN Push/PR/CI/Merge/Release. Keine bezahlten Calls/Uploads/Generationen und keine Provider-/Hostkonfiguration ausserhalb dieses Projekts. Keine Locked-Contract-/Pack-ABI-Abweichung. Nur Implementierung und statische Pruefung; lokal mit notwendiger Git-Eskalation committen, Completion-Matrix aktualisieren, /tmp/ngv-534-reviewfix-result.md liefern. Bei Spawn/Policy/echtem Vertragsblocker exakt melden und stoppen. TLDR.

ZUSAETZLICHER PARENT-BEFUND 12 (nach Opus-Review entdeckt, am Code bestaetigt): Der globale ModelCatalog.audio publiziert Mirelo-Modelle, GenerationView.audioModels nutzt diese unfiltriert und makeAudioRequest erstellt normale GenerationRequest. GenerationService.swift ca.923 behandelt .mirelo aber IMMER mit failBeforeSubmission("Mirelo audio runs through the host Mirelo operation path."). Damit ist ein sichtbarer, aktivierbarer manueller Audio-Generierungsweg garantiert kaputt, obwohl der neue Agent-Toolpfad funktioniert. Pruefe auch MusicTab und weitere gemeinsame Audio-Consumer. Behebe die vollstaendige Exponierung/Ausfuehrungs-Vertragsverletzung: angebotene Modelle muessen auf ihrem tatsaechlichen Nutzerpfad laufen, gemeinsame Ausfuehrung/Preflight/Kostenfreigabe wiederverwenden statt zweiten Client/einer zweiten Freigabe oder Fake-Erfolg. Nur exakt unterstuetzte Operationen/Controls anbieten; keine sichtbare Sackgasse oder bloss kosmetische Erklaerung. Der Issue verlangt den gemeinsamen Providerpfad und einen generischen Host-Consumer. Falls ein Consumer eine Option fachlich nicht ausdruecken kann, ist eine echte capability-basierte Filterung sinnvoll, aber die angebotenen Text/Video-SFX-Generationspfade duerfen nicht pauschal ins failBeforeSubmission laufen. Regression verbindet Katalogangebot mit dem echten ausgefuehrten Route-Handler.

Dokumentationskorrektur: Die erste Completion-Matrix nennt viele Kriterien 'Erfuellt' und einmal 'fixture-verifiziert', obwohl keine Tests ausgefuehrt wurden und bestaetigte Reviewfehler vorliegen. Aktualisiere diese Bezeichnungen ehrlich auf implementiert/source-geprueft bzw. Tests vorbereitet, Runtime/CI ausstehend. Keine nachgewiesene Doppelabrechnungsfreiheit oder vollstaendige End-to-End-Erfuellung behaupten, bevor die entsprechenden Tests tatsaechlich laufen. Public-Schemaabruf ist kein authentifizierter Account-Nachweis.

AKTUELLER FERTIGER INITIALCOMMIT: 4a58e36fa753182a63f1a886bdc83a0019d4f21c, Worktree sauber, erster Leaf beendet. Parent-Revalidierung danach:
- Required alternatives fehlt weiterhin; kein enforceGate/raw-toggle; kein in-flight Task-Coalescing; natives GenerationService .mirelo bleibt immer failBeforeSubmission; NoteJSON weiterhin withJSONObject: result; URLProtocol.body weiterhin nur httpBody. Das sind weiterhin gueltige Befunde.
- Bereits verbessert: operation wird vor completed early-return verglichen; genehmigte Recovery liegt vor Token/Katalog; mutationScope wird vor/innerhalb Ergebnisinstallationen geprueft; approve-Fehler geben unbenutzte Reservation frei; lesbare staging filenames. Nicht doppelt implementieren. Intent-/Options-/Quellenidentitaet bei cached/resume bleibt aber zu pruefen.
- Zusaetzliche Scope-Grenze bei FRISCHER Freigabe: Der execute-Closure awaitet aktuelle Preflight+Account, refresht den alten Record und ruft DANACH authorizeUnknownPaidOperation am eventuell inzwischen zu Projekt B gewechselten editor auf. Projekt-A IDs/workingRoot wurden vorher erfasst; die MutationScope darf nicht erst nach dem Wechsel fuer B entstehen und anschliessend A-Job autorisieren. Binde die Projektscope vor den ersten awaits und revalidiere nach asynchronen Schritten auch VOR Budgetreservation, Freigabe/Submission und allen spaeteren Writes. Resultatchecks allein reichen hier nicht.
