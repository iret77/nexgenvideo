Du bist IMPLEMENTIERUNGS-LEAF für #526. Nur der Parent darf nicht implementieren; du sollst selbst implementieren. Keine Delegation, Fleet, Agenten, AI-CLIs oder Reviewer. GPT-5.6 Sol xhigh. Exakte Basis und Worktree werden beim Start gesetzt.
Lies AGENTS/basic-memory, /tmp/ngv-526-current.json und /tmp/ngv-526-parent-scope.md. Beginne mit der exakten lokalen Integration von #520 d39dbeba0aea870b7f545262df1dd6b1b705e153 auf dem beim Start bestätigten #507-Stack; Konflikte unter Erhalt beider Änderungen lösen, niemals main mergen.
Implementiere den vollständigen Transcript-/Caption-/Marker-Index mit responsiver Suche, exakter Timeline-Navigation und schema-validiertem Vorschlagsreview samt explizitem atomarem Anwenden/Ablehnen/Undo. Quelle ist vorhandener kanonischer NexGenVideo-Code. Keine fremden Upstream-Repos lesen: automatische Freigabeprüfung blockierte das, Frage13 ist unbeantwortet. Keine Umgehung durch andere Tools/URLs/Clones. Die eigene Implementierung ist autorisiert und unabhängig davon möglich; Herkunft nicht behaupten.
Zuerst normative HTML-Spezifikation unter docs/ui mit vollständigen schmalen/breiten Zuständen erstellen, dann native Controls über AppTheme/#507 und bestehende Writer/Undo/Selection integrieren. Parent-Scope beschreibt konkrete Source-Time-/Snapshot-/Atomicity-Grenzen; alle Abnahmekriterien erfüllen. Kein zweiter Pipeline-Writer, keine erfundene Lineage, keine app-generierten Userturns. Native Interaktions- und lange-Timeline-Tests nur für Actions vorbereiten; keine FakePASS-Felder oder selbstbestätigenden Metadaten.
Keine Locked Specs/Pack-ABI ändern. Echte notwendige Abweichung konkret vorlegen und nur abhängigen Teil anhalten. Keine lokalen Builds/Compiler/Tests/App/Devserver/Providerprobes, kein Push/PR/CI/Rerun/Mainmerge/Release. Statisch prüfen, lokal eskaliert committen ohne AI-Trailer. Completion und PR-Entwurf in /tmp mit exakter SHA, Consumer-/Evidenzmatrix und ehrlichen Grenzen schreiben. TLDR.
