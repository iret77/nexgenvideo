# #153 — source/primary-doc preparation only

Live issue read2026-09-24 05:55 UTC, open, no comments/labels. Acceptance: selected generated/imported video + target aspect => subject-tracked reframed clip, rerunnable with provenance. Agent-authored owner-boundary prose is not a new mandate. No implementation/worktree yet.

Current repository: Timeline.Clip.cropTrack + cropAt(frame:) already carries keyframed crop with source/time mapping; FrameRenderer consumes it. Source trim/speed/orientation must be respected. Existing provider-tool generic route only accepts actual activated discovered prompt-free tools, rejects creative prompt arguments, always exact spend approval. Thus don't guess Higgsfield reframe endpoint or expose non-working provider setting. Native Apple Vision is a possible provider-neutral executable local tracking implementation, no purchased service needed; not yet chosen as final design.

Primary sources read by web search (no APIs executed): https://developer.apple.com/documentation/vision/trackobjectrequest — tracks a previously identified object over multiple frames, initializer supports frameAnalysisSpacing; https://developer.apple.com/documentation/Vision/tracking-multiple-objects-or-rectangles-in-video — actual initial object ROI drives tracking. Verify released macOS26 API and timing/coordinate semantics before implementation; no macOS27-only production path.

Concrete risks to solve: source-frame coordinates vs oriented display bounds, crop-fit aspect and dimensions, frame-rate/timebase incl variable source and timeline speed, multiple subjects/occlusion/cuts and confidence loss must pause/reselect or explicitly mark—not switch subjects silently; Cancel keeps original, project-switch rejects late results; locks and single Undo transaction; stored source/algorithm/ROI/aspect/output exact-byte provenance and durable rerun. Subject selection may be explicit user ROI; if creative model intent is accepted it MUST precompile through existing engine, never generic-provider-tool bypass. If producing rendered derived media use existing delivery/export engine and project-local import, not second inconsistent renderer. If keyframed crop alone does not meet 'reframed clip' acceptance, do not claim completion.

Before future leaf start choose dependency base deliberately, probably final551 + existing519 staticcrop + 528 export only if actual needed; don't blindly combine allPRs. Native UI must fit approved workbench and AppTheme; host canonical writers and Pack ABI preserved. Compiler/build/runtime/fixtures only prepared for Actions, no local execution.
