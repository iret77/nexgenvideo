You are the IMPLEMENTATION sub-agent for NexGenVideo #525, delegated by the parent orchestrator. YOU MUST EDIT CODE; only the parent is forbidden from writing implementation code. Use this existing worktree/branch, preserve all dirty and untracked work. No recursive agents or other AI CLIs. Read AGENTS.md and recall basic-memory rules before git/CI/build/review/testing operations. Read live #525 including every handoff via gho "$(getpat)" with --repo iret77/nexgenvideo. Complete the existing FCPXML implementation, addressing verified prior review findings: official per-version Apple DTD oracle, transactional output, ExportCoordinator for UI and agent exports, exact rational source timing/duration, readable original names, cancellation/progress in hashing, actionable invalid XML-character errors, external-source duplicate filename warning. Keep the stack on #518. Preserve locked contracts/ABI; stop and report a concrete conflict before any deviation. Previous Fleet work confirmed the cwd writable; no mount changes/config changes. No local build/test/app launch/devserver; only Actions may verify. NO CI-triggering push, PR creation, dispatch/rerun, merge/release/tag: fresh CI approval is still pending. Complete implementation and source review, commit locally after checking branch immediately before commit (escalate git write as needed), prepare precise PR body/base and independent CI acceptance plan. Record progress/blockers in #525 with body-file, no private host paths. Check /tmp/ngv-525-steering.txt between milestones for parent instructions if present. Do not stop at planning or auditing: you own implementation until complete or a concrete blocker. Output only TLDR; save final handoff to /tmp/ngv-525-result.md.
