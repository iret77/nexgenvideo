Du bist IMPLEMENTIERUNGS-LEAF für #537. Nur der Parent darf nicht implementieren; du sollst selbst implementieren. Keine Delegation, Fleet, andere Agenten, AI-CLIs oder Reviewer. GPT-5.6 Sol xhigh. Lies AGENTS/basic-memory/CONCEPT, /tmp/ngv-537-current.json und /tmp/ngv-537-parent-scope.md sowie relevante gesperrte Verträge.

Worktree und exakter sauberer Ausgang werden beim Start gesetzt. Vollständige Umsetzung von harmonisch begründetem Stimmungsverlauf aus vorhandenen lokalen Analyse-/Akkorddaten. Mirelo/#535 ist optional, nicht implementiert oder vorhanden voraussetzen. Kein neues Providerkonto/Keyfeld ohne echten Client und keine kostenpflichtige Probe.

Messung, Ableitung, Inferenz, Unsicherheit/Enthaltung und kreative Benutzerentscheidung konsequent trennen. Vorhandene genehmigte Sections/Trackhash/AlignmentProof nutzen; keine neuen Zeitgrenzen aus Lyrics/Stimmung. Versionierte, ABI-sichere Evidenzextension über den existierenden kanonischen record_affect-Writer mit eigenständig wiederholtem strukturellem Gate, exakter Lineage und bisheriger Brief-Phase-Capability. Keine Stored Fields an öffentlichen Pack-Typen, keine neue Scoringmechanik oder Gewichte, kein Dur=glücklich/Moll=traurig. Widersprüche zeigen. User-Override gewinnt im tatsächlichen Brief/PatternFit/Treatment/VisualArc-Consumer, automatische Deutung bleibt lesbar.

Vor Implementierung innerhalb deines Leafs kurz die gewählte ABI-/Artefakt-/Writer-Gate-Lösung in /tmp/ngv-537-design.md dokumentieren und eigenständig gegen Locked Specs prüfen. Keine zusätzliche Parent-Freigabe nötig, sofern vertragskonform. Falls echte Abweichung nötig: konkrete Entscheidungsprosa ausarbeiten, abhängige Änderung nicht implementieren. Die Parent-Scope-Datei ist Orientierung, kein Auftrag zu einem blind gewählten Datenformat.

Vollständige fachliche Fixtures und tatsächlichen App-/Agent-Consumer vorbereiten, keine fake subjektive Wahrheitsmetrik. Attribution des kreativen Reviews ausdrücklich von struktureller Gültigkeit trennen. Vorhandene Phase-Anleitungen in beiden Backendpfaden; UI nur AppTheme. Kein lokaler Compiler/Build/Test/App/Server/Providerprobe, kein Push/PR/CI/Rerun/Mainmerge/Release. Statisch prüfen und eskaliert lokal committen, kein AI-Trailer. /tmp/ngv-537-completion.md: exakter Stand, Kriterien->Consumer->vorbereitete Evidenz und echte Grenzen. TLDR.
