# #156 — preparation only

Live issue read2026-09-24 05:55 UTC, open, no comments/labels. Reference image/palette => actual generated/exportable .cube LUT applied to clip, exact provenance. No implementation/worktree yet.

Current actual consumers: Compositing/LUTLoader.swift validates .cube, stores by basename in ApplicationSupport/NexGenVideo/luts/project; EffectRegistry color.lut applies LUTTetraKernel; agent ToolExecutor+Color.swift apply_color calls same store. Existing #515/PR581 fixes65-point parsing on separate branch; future leaf must compose exact dependency, not overwrite with oldmain64limit. Current same-basename replacement and app-support-only truth need inspect against locked PROJECT_STORAGE/SaveAs; do not add new nonportable artifact roots or silently change locked storage.

No actual AI-LUT model/endpoint verified. Provider catalogs inspected have no concrete LUT route; generic provider MCP accepts prompt-free workflow tools only. Do not fabricate availability, call a generation with invalid probes, or relabel plain deterministic histogram matching as AI model generation. A schema-validated agent-designed color transform compiled through the prompt engine plus deterministic LUT sampling may satisfy the requested AI generation, but actual exact-byte prompt/model provenance and reference/palette binding must be designed and reviewed, not presumed. Must distinguish reference-look inference from deterministic LUT output; pin color space/domain/sampling and preserve effects. Actual applied clip and exportedLUT roundtrip must match. Current creative prompt gate is mandatory for model content inputs; no hidden raw route.

Concrete design/availability review needed before scheduling leaf; paid live generation not authorized. Scope includes native selection reference/palette, working progress/cancel, apply/undo, export, relocation and rerun lineage. No manual grading suite extension as substitute. All compile/runtime verification remains Actions-only.
