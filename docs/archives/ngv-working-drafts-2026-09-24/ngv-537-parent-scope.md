# #537 preparation — no implementation started

Live issue read 2026-09-24 around04:50UTC: OPEN, no comments. Main/origin main known8bd8fbde; verify before branching. No corresponding open PR in31 current entries. #535/#534 optional only; local harmony is a first-class input. No missing provider fact may be fabricated and no live paid call is authorized.

Parent source reads:
- Sources/MusicvideoPlugin/AnalysisSchema.swift: Analysis is public Pack-crossing value type; Chord(start,end,label), section IDs/boundaries, energy/tempo/other signal evidence already present. DO NOT add stored fields to these ABI-visible structs.
- Sources/MusicvideoPlugin/AffectProfile.swift: public struct detected/override/rationale/basis, .effective chooses usable user override; decoder ignores unknown keys, encoder drops extras. Do not add stored properties or casually round-trip a new extension through this encoder.
- Sources/NexGenVideo/Agent/Tools/ToolExecutor+Workflow.swift: recordAffectTool writes analysis/affect.json and suggestPatterns passes full JSON to provider; tool bound to Brief by ToolDefinitions/PipelineAgentContract. Existing basis measured/documented/inferred is dangerously broad for emotional interpretation; keep measured signal separate from inferred emotion, confidence capped by locked contract. Need ensure old baseline backward compatibility without relabeling emotion measured.
- ToolDefinitions.swift ~1160: existing record_affect schema, fixed vocabulary. Extend the existing writer with independently validated versioned evidence, not a second new writer outside canonical set.
- PipelineExecutionPlanComposer brief output explicitly includes affect.json exact bytes; source analysis cumulative lineage already exists. A separately owned extension artifact would need explicit output/fingerprint participation; embedding a versioned JSON envelope may fit existing artifact without new file contract, but verify compatibility and preservation through all old consumers before choosing. No parent-mandated architecture shortcut.
- Engine/Artifacts/MusicvideoProductionExtensionsV1.swift: current VisualArc section/motif/creative-decision consumers from #489, public types cannot grow stored fields while compatibility unchanged. Reuse ABI-safe existing carrier or new independent type.
- Sources/MusicvideoPlugin/AnalysisMeasurementProof.swift: exact song hash + optional alignment source/lyrics/alignment fingerprint, timingEvidence; timed lyrics only when actual proof permits. Raw lyric prose cannot invent timing.
- Packaged brief/treatment/shotlist phase documents and existing PatternFit policy/normalization unchanged; mandatory instructions in both agent backends and current phase capability sets.

Locked PatternFit read in full. New emotion must remain bounded inference, not binary chord-major-happy rule or normalized uncalibrated raw RMS. Existing affect tags, scorer weights, evidence min confidence, coverage and explicit user-choice dominance stay unchanged. Unknown/contradictory signals not averaged into certainty. ReadAnalysis+proof only approved current exact source. Source changes invalidate downstream claims/gates; canonical writer+independent gate validates exact cited fields/segments/timestamps/hashes and preserves explicit user override intent.

Acceptance is concrete: same progression different dynamics/instrumentation, low-energy bright harmony, high-tempo dark harmony, conflicting aligned lyrics, instrumental/no chords/bad key/unreliable lyrics/provider failure. Explain support and conflict, section and track views, evidence attribution and abstention; actual Treatment/VisualArc consumer decision must use effective desired affect while detected evidence remains visible. Structural deterministic tests are not subjective-quality proofs; attributed creative review must remain distinct. Native UI AppTheme, no test/build/app locally.

No lockedspec deviation presumed necessary, but if implementation cannot fit existing record_affect/Brief ownership and ABI-safe extension, leaf must produce concrete proposal and STOP dependent implementation. #216/#559/#546 pending decisions not adopted. #535 nonexistent data optional unavailable, no mandatory Mirelo setup.
