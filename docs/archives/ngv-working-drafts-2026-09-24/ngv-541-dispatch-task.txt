Du bist IMPLEMENTIERUNGS-LEAF für zwei begrenzte #541-Restpunkte. Nur der Parent darf nicht implementieren; du sollst selbst implementieren. Keine Delegation/Fleet/Agents/AI-CLIs/Reviewer. Lies AGENTS.md/basic-memory. Saubere exakte Basis 1ba7293231692370d0492d2d3c35ff016dfbb56d auf codex/issue-541. Alle bisherigen Korrekturen erhalten.

1 Tatsächlich ausführbarer Pre-Merge-CI-Einstieg. Beide neuen Kandidatenworkflows existieren nicht auf main8bd8fbde. GitHub Primärdokumentation verlangt workflow_dispatch auf Defaultbranch: https://docs.github.com/actions/managing-workflow-runs/manually-running-a-workflow und https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#onworkflow_dispatch . Mainmerge zur Registrierung ist NICHT genehmigt. Bereite deshalb einen vorhandenen, auf main registrierten manuellen Workflow als strikt begrenzten Einstieg vor (z.B. bundle.yml mit expliziten Evidence-Modi und passenden workflow_call-Unterworkflows, oder gleichwertig). --ref später reviewed Branch muss ausschließlich den gewählten source-only Ubuntu- oder binary-metadata macOS-Job ausführen können, ohne vollständige CI/Bundle/Signierung/Release/Provideraufrufe. Defaultverhalten aller bestehenden Aufrufer erhalten. Exakte Quell-SHA und Workflowref binden, enge Inputwerte, Outputklassifikation/no-public-binary-Guard erhalten. Keine automatischen Trigger hinzufügen, keine Trialdispatches. Dokumentiere zwei konkrete später nutzbare GH-Befehle samt tatsächlichem Jobumfang; sie sind keine Ausführungsfreigabe.

2 Supervisor-Cleanup: Parent sieht in terminateAndReapOwnedChild bei fehlendem startAbsoluteTime nur process.terminate() gefolgt von unbeschränktem waitUntilExit. Wenn Identityabfrage fehlschlägt während eigener Worker weiterläuft und SIGTERM ignoriert, hängt Supervisor statt begrenzt zu bereinigen. Prüfe diesen echten Fehlerpfad und sichere direkte Kindownership + begrenztes Ende auf allen Pfaden. Keine fremden PIDs/Scans/Prozessgruppen/hostglobalen Eingriffe, keine theoretische perfekte PID-Atomarität erfinden. Nutze belastbare tatsächliche Own-Child-Mechanik; falls notwendige Umstellung, klein/fachlich bleiben. Bereite gezielten Actionsfall Identityaufnahmefehler + ignoriertes TERM + gesunder Folgejob vor; nichts lokal ausführen. Das inzwischen korrigierte identity.write-defer und die korrekte signal-Regel nicht regressieren.

Die neue separate bpy-binary-evidence-candidate.yml schließt den zuvor notierten Zirkularitätsfehler; nicht erneut als fehlend melden. Sourcepaket enthält bisher Quellen+Notice+RECORD/Metadata, keine mitgepackten Wheelbinaries, dies erhalten. Quellekorrespondenz-/Noticelücken bleiben ehrlich offen bis echte Evidenz vorliegt, keine neue externe Legal-/Attestfreigabe.

Keine LockedSpecs/Pack-ABI ändern. Keine lokalen Builds/Tests/Compiler/App-/Worker-/Sandboxstarts/Devserver. Kein Push/PR/CI/Rerun/Dispatch/Merge/Release. Statisch prüfen, lokal via Eskalation committen. /tmp/ngv-541-dispatch-result.md plus Completion aktualisieren, genaue Grenzen und vorbereitete Befehle. TLDR.
