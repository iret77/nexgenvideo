Du bist IMPLEMENTIERUNGS-LEAF für die bestätigten Restbefunde #507. Nur der Parent darf nicht implementieren; du sollst selbst implementieren. NIEMALS ai-fleet, Agents/Subagents, andere AI-CLIs oder Reviewer starten. AGENTS.md/basic-memory befolgen. HEAD 395d1f49d33f3a7d58062ffc57ac1b75dc02a288 und gesamter Stack auf #506 erhalten.

Lies /tmp/ngv-507-axis-review.json, /tmp/ngv-507-axis-result.md und /tmp/ngv-507-current.json. Parent bestätigt die ersten drei Befunde source-seitig. Behebe diese Fehlerklasse vollständig:
1. Crop benutzt noch propertyRow mit Keyframecontrols IM trailing-HStack, zusätzlich zur leeren reservierten Zubehörspalte. Migriere Crop in denselben echten Control-/Accessory-Vertrag wie andere animierbare Zeilen. Toggle/Menu auf Controlseite, Keyframecontrols im Accessoryslot; Disabled/Mixed/Opacity/Handler/Undo behalten.
2. Schmales geöffnetes Keyframepanel: AppTheme.Layout.inspectorMin=150, Padding und horizontale Aufteilung können Controlspalte auf rund50pt schrumpfen; fixedSize-Felder erzeugen negative Ursprünge/Clipping. Implementiere zentralen adaptiven Aufbau für Audio und Video: bei unzureichender Gesamtbreite keine unbrauchbare halbierte Spalte, z.B. Keyframepanel unter die Controls; zusätzlich Zeilen-Fallback, wenn Control+Accessory nicht in verfügbare Breite passen. Reines x-Clamping mit abgeschnittenem Wert ist keine Lösung. Alle Controls und essenziellen Texte müssen erreichbar/lesbar bleiben. Werte über AppTheme. Keine zusätzlichen tabweisen Padding-Flickwerke.
3. Geometrietest prüft derzeit offen/geschlossen zweimal mit derselben rows.frame(width:rowWidth), künstlich verdoppeltem Host; das ist keine reale Aufteilung. Prüfe tatsächliche Gesamtbreite inkl inspectorMin=150 und184/260/360 sowie ausreichendem breiten Fall; nutze produktiven gemeinsamen Aufbau statt unechter Splitkopie. Skalen100/130/150, beidePanelzustände, kein reservierterSlot bei MultiSelection/normalenText-/Caption-Zeilen. Crop und Position einbeziehen, Feldrahmen und tatsächliches minX/maxX/Containment prüfen, nicht Gesamtcontainer. Keine Tests, die nur interneImplementierung wiederholen.

Reviewpunkte4/5 sind niedrige hypothetische bzwunsichere Hinweise, keine belegten neuen Produktfehler: aktuelle ViewBuilder liefern genau eine Controlview; PositionX/Y-Suffix war bereits so. Keine unnötigeGeneralisierung oder neueUserentscheidung. Beim zentralenLayout kannst du stabile Einzelcontainer/Rollen verwenden, wenn das die reale Korrektur vereinfacht; Position muss im echten Narrowtest jedenfalls lesbar enthalten bleiben. EmptyView-Guard wurde bereits behoben, nicht wieder regressieren. Bestehende numerischeHandler, Bereiche, Undo, Keyframes und Mixedwerte erhalten.

NativeAcceptance echtenToggle/zehnFallnamen erhalten, um narrow und adaptive offenePanels sinnvoll erweitern, falls erforderlich. Tests/Harness nur vorbereiten. Keine lokalen Builds,Tests,SwiftCompiler,Appstarts,Devserver. Kein Push/PR/CI/Rerun/Merge/Release. Kein LockedContract-/PackABI-Bruch; keinVersions-/Changelogbump. Statisch prüfen/git diff --check, lokal committen viaEskalation. /tmp/ngv-507-finalfix-result.md mitSHA, korrekterEvidenzgrenze undaktualisiertemPRBodyEntwurf erstellen. Parent führt unabhängigen Delta-Review aus. BeiSpawnfehler STOP undexakteMeldung. TLDR.
