# Self-review: production workbench

Source-based review, 2026-09-13. Native app not executed.

1. High: Audio Analysis regression. AnalysisPanelView.swift renders stat bindings, BeatTimeline, StructureHierarchyList, review-required/unresolved/degraded diagnostics. PackSurfacePrimitives.swift:131 shows proportional section bands, beats and emphasized downbeats. prior-workbench.js phaseSummary replaces this with three text boxes and prose. Restore a large aligned time surface, selected section detail, measured provenance and missing-data states. Do not fabricate energy measurements or nested hierarchy when absent.
2. High: Document phases are form-first. phaseFields renders every property as a textarea and requires all fields; phaseSummary Brief and Treatment present long text without selection. Use a selectable causal sequence and compact direction controls; preserve prose only for actual writing. Concept illustrations must not be misrepresented as approved future storyboard or reference assets.
3. High: Inspector repeats generic instructions instead of exposing the selected object. Workspace phase renderer replaces selection context with Source/Review focus/Revision prose. Make selection the organizing unit: section, beat, reference, camera, frame or take.
4. Medium: AI refinement replaces text with one predetermined paragraph, while visual changes remain invisible. Use bounded example comparisons tied to selected artifacts, visible before/after, accept/reject with version invalidation. Label simulation accurately.
5. Medium: Frame/take detail shows only one candidate and prose; no plan-to-render comparison. Keep sketch, production image and generated take roles distinct; compare existing examples without claiming real generation or video.
6. Medium: Many wrapping render functions and duplicate actions obscure the interaction contract. Reduce duplicated presentation, keep one persistent approval/recovery dock and existing deterministic guards.
7. Medium: Visual concept must retain existing strengths: reference thumbnail categories, linked shot identities, source routes, blocking plan/elevation, media provenance, pack accent and native pane geometry. Current shot plan and storyboard are views of the same timed shot records. Blocking changes remain within existing phase ownership.

Revision direction: film workbench, not dashboard. A large visual document for each phase; synchronized selection and contextual controls. Audio first; no waveform/energy invented as native ground truth. Music pack adds audio surface; generic workflow remains complete. Preserve startup order, one active job, review lineage, rewind and budget gates. Existing thumbnails are illustrative and must be identified as such. Do not modify native code or locked contracts.
